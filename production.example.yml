version: "3"

services:
  # Discord bot - production
  discord-bot:
    image: "ghcr.io/dutchdronesquad/dds-bot:latest"
    environment:
      BOT_TOKEN: ""
      OWNER_ID: ""
    deploy:
      mode: replicated
      replicas: 1
      restart_policy:
        condition: on-failure
        delay: 1m